
export interface Players {
    raidId : string;
    profileId : string;
    level : string;
    team : string;
    name : string;
    group : number;
    spawnTime : number;
    mod_SAIN_brain : string;
    type : string;
};

async function NoOneLeftBehind(raidId, profileId, players : Players[]) {

    // Get all the players in the db

    
    // Filter out the ones already there


    // Insert the missing players


    // Call it a day

}