module.exports = {
    config : {
        accountId: '', // Replace with your Cloudflare account ID
        namespaceId: '', // Replace with your actual KV namespace ID
        apiToken: '' // Replace with your Cloudflare API token with KV read permissions
    }
}